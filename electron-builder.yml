appId: com.talentcombinator.app
productName: Talent Combinator
artifactName: "${productName}-${version}-${os}-${arch}.${ext}"
directories:
  buildResources: build
  output: dist
files:
  - out/**/*
  - "!node_modules/**/*"
mac:
  target:
    - dmg
    - zip
  category: public.app-category.utilities
win:
  target:
    - nsis
linux:
  target:
    - AppImage
    - deb
publish:
  provider: github
  releaseType: release
